[metadata]
license_files = LICENSE

[flake8]
max-line-length = 100
extend-ignore = E203, W503
exclude = .venv,venv,build,dist

[isort]
profile = black
line_length = 100

[tool:pytest]
addopts = -q
testpaths = tests
